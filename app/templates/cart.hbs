<h2>Cart</h2>

{{#if this.model}}
{{#each this.model as |item|}}
<CartCard @item={{item}}>
    {{!-- <p>Qty: {{item.qty}}</p> --}}
    <p>Total Amount: {{format-currency item.amount}}</p>
</CartCard>
{{/each}}
{{else}}
<p>Your cart is empty</p>
{{/if}}

{{outlet}}